import 'package:flutter/material.dart';

class CredentialLoginScreen extends StatelessWidget {
  const CredentialLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Implement credential login page'),
      ),
    );
  }
}
